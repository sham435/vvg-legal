[build]
builder = "NIXPACKS"

[build.args]
APP_DIR = "backend"

[deploy]
startCommand = "cd backend && npm run start:prod"
healthcheckPath = "/health"
healthcheckTimeout = 30

[deploy.variables]
PORT = "3000"
